# SAI DNS Filtering Rules

# Block known tracking domains
0.0.0.0 telemetry.example.com
0.0.0.0 ads.example.com
0.0.0.0 analytics.example.com

# Block data exfiltration endpoints
0.0.0.0 pastebin.com
0.0.0.0 requestbin.net

# Allow everything else by default (handled by system DNS resolver)
